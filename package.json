{
  "name": "dnd-telegram-bot",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "bower install && grunt build",
    "start": "node -r dotenv/config app.js dotenv_config_path=config/.env"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^8.2.0",
    "requests": "^0.3.0",
    "slimbot": "git+https://github.com/edisonchee/slimbot.git"
  }
}
